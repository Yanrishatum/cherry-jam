package ldx.comps;

import h2d.Flow;

class LocaleSelect extends Flow {
  
  public function new(?parent) {
    super(parent);
    maxHeight = 20;
    lineHeight = 20;
    horizontalSpacing = 4;
    verticalAlign = Top;
  }
  
}